# Public — safe for the browser bundle
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never prefix with NEXT_PUBLIC_, never call from the client
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
